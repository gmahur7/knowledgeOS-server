# Enforce pnpm as the package manager for this project.
engine-strict=true

# Keep node_modules layout strict (no phantom deps). This is pnpm's default,
# set explicitly so the contract is visible.
node-linker=isolated
